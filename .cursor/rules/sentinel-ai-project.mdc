---
description: Sentinel AI 项目的设计方案、技术决策和上下文，在处理该项目相关任务时自动应用
globs: 
alwaysApply: true
---

# Sentinel AI — 项目设计文档

## 项目概述

**项目名称：** Sentinel AI
**目标：** 扫描 Java 项目中的所有 SQL，结合表结构和生产数据量，判断是否走索引、是否为潜在慢 SQL，并结合 AI 给出优化建议。
**项目路径：** `e:\cursor\sentinel-ai\`
**GitHub 仓库：** `https://github.com/prove0603/sentinel-ai`
**GroupId：** `com.zhuangjie`
**包名：** `com.zhuangjie.sentinel`
**作者：** 庄杰 (Zhuang Jie)
**项目定位：** 独立的 Spring Boot Web 项目（含 WebUI），便于展示和后续开源扩展。

---

## 技术栈

| 层面 | 技术 | 版本 |
|------|------|------|
| Java | Java 21 LTS | 21 |
| 后端框架 | Spring Boot | 3.4.3 |
| AI 框架 | Spring AI Alibaba (DashScope) | 1.1.2.1 |
| AI 模型 | 通义千问 Qwen-Max / Qwen-Turbo | - |
| ORM | MyBatis-Plus | 3.5.7 |
| SQL 解析 | JSqlParser | 5.0 |
| Java 源码 AST | JavaParser | 3.26.2 |
| Git 增量检测 | JGit | 7.1.0 |
| 缓存 | Caffeine | 3.1.8 |
| 工具 | Hutool, Guava | latest |
| 前端 | Vue 3 + Vite + Element Plus + TypeScript | Vue 3.5, Vite 6 |
| 数据库 | MySQL (prod) / H2 (dev) | - |
| 构建 | Maven 多模块 | - |

---

## 项目结构

```
sentinel-ai/
├── pom.xml                          # 父 POM
├── sentinel-ai-server/              # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/zhuangjie/sentinel/
│       ├── SentinelAiApplication.java
│       ├── config/                  # 配置类
│       │   ├── AiConfig.java
│       │   ├── CacheConfig.java
│       │   ├── MybatisPlusConfig.java
│       │   ├── MybatisPlusMetaObjectHandler.java  ← P1 新增
│       │   └── WebConfig.java
│       ├── common/                  # Result, PageResult 等
│       ├── controller/              # REST API
│       ├── service/                 # 业务逻辑
│       ├── scanner/                 # SQL 提取引擎 ← P1 新增
│       │   ├── SqlScanner.java              # 扫描器接口
│       │   ├── MapperXmlScanner.java        # Mapper XML 扫描实现
│       │   ├── DynamicSqlExpander.java      # 动态 SQL 展开
│       │   └── SqlNormalizer.java           # SQL 标准化 + 哈希
│       ├── analyzer/                # 规则引擎 + AI 分析 ← P1 新增
│       │   ├── RuleBasedAnalyzer.java       # 规则引擎编排器
│       │   ├── AnalysisResult.java          # 分析结果 record
│       │   └── rules/              # 规则集 (sealed interface + record)
│       │       ├── SqlRule.java             # sealed interface
│       │       ├── RuleViolation.java       # 违规 record
│       │       ├── FullTableScanRule.java
│       │       ├── LikeLeadingWildcardRule.java
│       │       ├── ImplicitConversionRule.java
│       │       ├── LargeJoinRule.java
│       │       ├── DeepPaginationRule.java
│       │       └── NoConditionDynamicSqlRule.java
│       ├── delta/                   # Git 增量检测 (P3)
│       ├── knowledge/               # RAG 知识库 (P5)
│       ├── db/
│       │   ├── entity/             # 实体类
│       │   ├── mapper/             # Mapper 接口
│       │   └── service/            # DB Service (ServiceImpl)
│       └── pojo/
│           ├── dto/                # record DTO
│           ├── vo/                 # record VO (P2+)
│           └── enums/              # 枚举
├── sentinel-ai-ui/                  # Vue 3 前端
│   ├── package.json
│   ├── vite.config.ts              # 代理 /api -> localhost:8090
│   └── src/
│       ├── views/                  # 页面组件
│       ├── components/             # 公共组件
│       ├── api/                    # API 封装 (axios)
│       ├── router/                 # Vue Router
│       └── styles/
└── .cursor/rules/
```

---

## 核心模块

### Scanner (SQL 提取引擎) — ✅ P1 已实现
- `SqlScanner` - 扫描器接口，定义 `scan(Path projectRoot)` 方法
- `MapperXmlScanner` - ✅ 解析 mapper.xml，递归遍历项目目录，提取 SQL，解析 `<sql>` 片段用于 `<include>` 引用
- `DynamicSqlExpander` - ✅ 展开动态 SQL 的执行路径，支持双模式（ALL_TRUE/ALL_FALSE）
  - 处理: `<if>`, `<choose>/<when>/<otherwise>`, `<where>`, `<set>`, `<trim>`, `<foreach>`, `<include>`, `<bind>`
- `SqlNormalizer` - ✅ SQL 标准化（替换 `#{}/${}` → `?`，归一化空白）+ SHA-256 哈希
- `QueryWrapperScanner` - 🔲 P4 阶段：JavaParser AST 分析 QueryWrapper/LambdaQueryWrapper
- `AnnotationSqlScanner` - 🔲 P4 阶段：扫描 @Select/@Insert 等注解 SQL

### Delta (增量检测) — 🔲 P3 阶段
- `GitDeltaDetector` - JGit 获取 commit diff
- `SqlHashComparator` - SQL 标准化后 hash 对比

### Analyzer (分析引擎) — ✅ P1 规则引擎 + P2 AI 分析已实现
- `RuleBasedAnalyzer` - ✅ 规则引擎编排器（同步，毫秒级），JSqlParser 解析 SQL AST 后运行所有规则
- `AnalysisResult` - ✅ 分析结果聚合 record（violations + 最终风险等级）
- `AiSqlAnalyzer` - ✅ P2 已实现：Spring AI Alibaba 深度分析
  - Prompt 工程：包含 SQL 文本/类型/来源/规则引擎发现，要求 JSON 结构化回复
  - 结构化输出：AI 返回 JSON → Hutool 解析为 `SqlRiskAssessment` record
  - Caffeine 缓存：以 sqlHash 为 key 缓存 AI 结果，避免重复分析
  - 容错重试：最多 1 次重试，指数退避
  - `AiAnalysisDetail` 内部 record 包装 assessment + model + tokensUsed
- 规则集 (sealed interface `SqlRule` + record 实现):
  - `FullTableScanRule` ✅ — SELECT/UPDATE/DELETE 无 WHERE → P0
  - `LikeLeadingWildcardRule` ✅ — LIKE '%xxx' 或 CONCAT('%',...) → P1
  - `ImplicitConversionRule` ✅ — WHERE 中对列使用函数包裹（YEAR/CAST/DATE_FORMAT 等）→ P2
  - `LargeJoinRule` ✅ — ≥3 表 JOIN P2，≥5 表 P1
  - `DeepPaginationRule` ✅ — OFFSET ≥1K P2，≥10K P1，≥100K P0
  - `NoConditionDynamicSqlRule` ✅ — 动态 SQL 所有条件可能为空 → P0

### AI 集成 (Spring AI Alibaba) — ✅ P2 已实现
- 使用 `spring-ai-alibaba-starter-dashscope` 对接通义千问
- `ChatClient` + system prompt（资深 MySQL DBA 角色）
- `AiConfig` 用 `@ConditionalOnProperty(sentinel.ai.enabled=true)` 控制 ChatClient 创建
- AI 调用策略：规则前置过滤（仅 P0/P1/P2）→ Caffeine 缓存 → AI 调用 → JSON 解析 → 更新分析记录
- 结果写入 `t_sql_analysis` 的 ai_* 字段，finalRiskLevel 取规则+AI 中更严重的等级

---

## 数据库表

```
t_project_config   -- 项目配置
t_table_meta       -- 表结构元数据
t_sql_record       -- SQL 记录
t_scan_batch       -- 扫描批次
t_sql_analysis     -- 分析结果
```

---

## API 路径

- `/api/project/*` - 项目管理
- `/api/scan/*` - 扫描触发/历史
- `/api/analysis/*` - 分析结果
- `/api/dashboard/*` - 仪表盘

---

## 开发约定

- 代码结构：controller → service → db.service → mapper
- 统一响应：`Result<T>` 和 `PageResult<T>`
- 虚拟线程：`spring.threads.virtual.enabled=true`
- Java 21 特性：
  - `record` 用于 DTO/VO/AI 结构化输出
  - `sealed interface` + `record` 用于规则类体系
  - `pattern matching for switch` 用于类型分发
  - 文本块 (`"""`) 用于 AI Prompt 模板
- AI 调用缓存：Caffeine (key = sql_hash + table_meta_hash)
- 开发环境用 H2 数据库 (文件模式)，生产用 MySQL
- 前端开发代理：Vite `proxy /api -> http://localhost:8090`
- 前端 build 输出到 `sentinel-ai-server/src/main/resources/static/`

---

## 实施路线图

- **P1（MVP）：** XML Mapper 扫描 + 规则引擎 + 基础 WebUI ← **✅ 已完成并验证**
  - ✅ MapperXmlScanner + DynamicSqlExpander + SqlNormalizer
  - ✅ RuleBasedAnalyzer + 6 条规则（sealed interface + record）
  - ✅ ScanService 完整扫描→分析→持久化流程
  - ✅ MybatisPlusMetaObjectHandler 自动填充时间
  - ✅ 已用真实项目验证全量扫描
- **P2：** Spring AI Alibaba 集成 + Prompt 工程 + 结构化输出 ← **✅ 后端已完成，待配置 API Key 测试**
  - ✅ AiSqlAnalyzer — Prompt 工程 + JSON 结构化输出 + Caffeine 缓存 + 容错重试
  - ✅ AiConfig — @ConditionalOnProperty 控制 AI 开关
  - ✅ ScanService 串联：规则引擎 → AI 深度分析（仅 P0/P1/P2） → 结果回写
  - ✅ 配置：sentinel.ai.enabled + AI_DASHSCOPE_API_KEY 环境变量
  - 🔲 配置通义千问 API Key 进行端到端测试
- **P3：** JGit 增量扫描
- **P4：** JavaParser 解析 QueryWrapper/LambdaQueryWrapper
- **P5：** RAG 知识库
- **P6：** 完善 Dashboard、通知、导出报告、开源准备

---

## 环境信息

- **JDK 21 路径：** `C:\Users\zhuangjie10\.jdks\jdk-21`（Temurin 21.0.6）
- **构建时设置：** `$env:JAVA_HOME = "$env:USERPROFILE\.jdks\jdk-21"` 然后 `mvn compile`
- **后端端口：** 8090
- **前端开发端口：** 5173（`npm run dev` in sentinel-ai-ui）
- **H2 控制台：** http://localhost:8090/h2-console（dev 环境）
- **数据库文件：** `./data/sentinel-ai.mv.db`（H2 文件模式，运行目录下自动创建）

---

## 注意事项

- GroupId 使用 `com.zhuangjie`，不要使用 `com.al.risk`（开源合规）
- 项目名 sentinel-ai，不包含公司相关标识
- 代码和注释中不涉及公司内部信息
- 新对话框中可用 `@sentinel-ai-project` 引用此规则文件获取完整项目上下文
